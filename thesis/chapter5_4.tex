%%=============================================================
%% chapter5_4.tex
%% Phần 5.4 — Kết quả đánh giá: AI #3 Phát hiện vật thể
%%
%% \input file — cần thêm vào preamble:
%%   \usepackage{tikz}
%%   \usetikzlibrary{arrows.meta,positioning,patterns}
%%   \usepackage{pgfplots}
%%   \pgfplotsset{compat=1.18}
%%   \usepackage{booktabs,multirow,subcaption,xcolor}
%%=============================================================

% ================================================================
\section{Kết quả và Đánh giá: AI \#3 --- Phát hiện vật thể}
\label{sec:ai3-results}
% ================================================================

% ----------------------------------------------------------------
\subsection{Kết quả detection model: COCO metrics}
\label{subsec:ai3-coco}
% ----------------------------------------------------------------

Mô hình được đánh giá trên cả validation set và test set độc lập
theo chuẩn \textbf{COCO evaluation protocol}.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      ybar, bar width=0.45cm,
      width=10cm, height=7cm,
      symbolic x coords={mAP (0.50:0.95), AP50, AP75, AR},
      xtick=data,
      x tick label style={font=\small, align=center},
      ymin=0, ymax=1.15,
      ylabel={Score},
      ylabel style={font=\small},
      legend style={at={(0.5,1.03)}, anchor=south, legend columns=2, font=\small},
      legend cell align=left,
      nodes near coords,
      every node near coord/.style={font=\scriptsize,
        /pgf/number format/fixed, /pgf/number format/precision=3},
      enlarge x limits=0.25,
      grid=major, grid style={dashed, gray!30},
      axis line style={gray!60},
    ]
    \addplot[fill=cppblue!70, draw=cppblue!90] coordinates {
      (mAP (0.50:0.95), 0.791)
      (AP50, 0.990)
      (AP75, 0.990)
      (AR,   0.869)
    };
    \addplot[fill=orng!80, draw=orng!90] coordinates {
      (mAP (0.50:0.95), 0.799)
      (AP50, 1.000)
      (AP75, 0.969)
      (AR,   0.856)
    };
    \legend{Validation, Test}
    \end{axis}
  \end{tikzpicture}
  \caption{COCO metrics trên validation và test set. AP50 = 1.000
           trên test cho thấy 100\% cylinder được phát hiện đúng
           tại ngưỡng IoU lỏng lẻ (50\%).}
  \label{fig:ai3-coco}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{COCO metrics và detection report trên test set}
  \label{tab:ai3-metrics}
  \small
  \begin{tabular}{lcc}
    \toprule
    \textbf{Metric}                    & \textbf{Validation} & \textbf{Test} \\
    \midrule
    mAP (IoU 0.50:0.95)               & 0.791               & \textbf{0.799} \\
    AP50 (IoU $\geq$ 0.50)            & 0.990               & \textbf{1.000} \\
    AP75 (IoU $\geq$ 0.75)            & 0.990               & 0.969 \\
    AR (maxDets=100)                   & 0.869               & 0.856 \\
    \midrule
    Precision (IoU=0.50, conf=0.50)    & ---                 & \textbf{1.000} \\
    Recall                             & ---                 & 0.990 \\
    F1-Score                           & ---                 & \textbf{0.995} \\
    TP / FP / FN                       & ---                 & 95 / 0 / 1 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      ybar, bar width=0.55cm,
      width=8.5cm, height=6cm,
      symbolic x coords={Precision, Recall, F1-Score},
      xtick=data,
      x tick label style={font=\small},
      ymin=0.96, ymax=1.04,
      ylabel={Score},
      ylabel style={font=\small},
      nodes near coords,
      every node near coord/.style={font=\small,
        /pgf/number format/fixed, /pgf/number format/precision=3},
      enlarge x limits=0.3,
      grid=major, grid style={dashed, gray!30},
    ]
    \addplot[fill=pygreen!65, draw=pygreen!80] coordinates {
      (Precision, 1.000)
      (Recall,    0.990)
      (F1-Score,  0.995)
    };
    \end{axis}
  \end{tikzpicture}
  \caption{Precision, Recall và F1-Score trên test set
           (PASCAL VOC protocol, IoU$\geq$0.50, conf$\geq$0.50,
           chỉ lớp cylinder). Precision = 1.000, FP = 0.}
  \label{fig:ai3-prf}
\end{figure}


% ----------------------------------------------------------------
\subsection{Phân tích confidence score: true positive vs false positive}
\label{subsec:ai3-conf}
% ----------------------------------------------------------------

Một yếu tố quan trọng khi chọn ngưỡng confidence là
khoảng cách phân tách giữa true và false positive. Nếu
khoảng này hẹp, ngưỡng khó đặt; nếu rộng, hệ thống có
biên an toàn cao.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=11cm, height=5.5cm,
      xlabel={Confidence score},
      ylabel={},
      xmin=0, xmax=1.05,
      ymin=0, ymax=3,
      xtick={0,0.1,...,1.0},
      ytick={0.7, 2.0},
      yticklabels={
        \small False Positive (cube, $n{=}10$),
        \small True Positive (cylinder, $n{=}218$)
      },
      y tick label style={align=right, font=\small},
      x tick label style={font=\small},
      xlabel style={font=\small},
      grid=both, grid style={dashed, gray!25},
      axis line style={gray!60},
    ]

    %% ---- True Positive (cylinder) ----
    \addplot[only marks, mark=|, mark size=5pt,
             mark options={draw=pygreen!70, line width=1.2pt},
             opacity=0.55]
    coordinates {
      (0.29,2)(0.61,2)(0.74,2)
      (0.82,2)(0.84,2)(0.85,2)(0.86,2)(0.87,2)(0.88,2)
      (0.89,2)(0.90,2)(0.91,2)(0.92,2)(0.93,2)(0.94,2)
      (0.95,2)(0.96,2)(0.97,2)(0.98,2)
    };

    %% ---- False Positive (cube) ----
    \addplot[only marks, mark=|, mark size=5pt,
             mark options={draw=warnred!70, line width=1.2pt},
             opacity=0.65]
    coordinates {
      (0.08,0.7)(0.13,0.7)(0.17,0.7)(0.21,0.7)
      (0.25,0.7)(0.28,0.7)(0.31,0.7)(0.33,0.7)(0.35,0.7)(0.36,0.7)
    };

    %% ---- Threshold line ----
    \addplot[thick, dashed, draw=gray!80]
    coordinates {(0.50,0) (0.50,3)};
    \node[font=\scriptsize, text=gray!80, rotate=90]
         at (axis cs:0.52, 1.5) {threshold = 0.50};

    %% ---- Gap annotation ----
    \draw[->, >=Stealth, thick, gray!60]
         (axis cs:0.43, 2.55) -- (axis cs:0.50, 2.55);
    \draw[->, >=Stealth, thick, gray!60]
         (axis cs:0.43, 2.55) -- (axis cs:0.36, 2.55);
    \node[font=\scriptsize, text=gray!70]
         at (axis cs:0.43, 2.72) {gap = 0.34};

    %% ---- Median TP ----
    \addplot[only marks, mark=diamond*, mark size=4pt,
             mark options={draw=pygreen!90, fill=pygreen!55}]
    coordinates {(0.94, 2)};
    \node[font=\scriptsize, text=pygreen!80!black, above]
         at (axis cs:0.94, 2.05) {median 0.94};

    %% ---- Max FP ----
    \addplot[only marks, mark=diamond*, mark size=4pt,
             mark options={draw=warnred!80, fill=warnred!50}]
    coordinates {(0.36, 0.7)};
    \node[font=\scriptsize, text=warnred, below]
         at (axis cs:0.36, 0.65) {max 0.36};

    \end{axis}
  \end{tikzpicture}
  \caption{Phân tách confidence score: true positive (xanh lá)
           so với false positive (đỏ). Mỗi vạch đưứng đại diện
           một detection. Ngưỡng 0.50 nằm giữa khoảng gap 0.34,
           loại bỏ toàn bộ false positive (0/10) trong khi
           giữ lại 99.5\% true positive.}
  \label{fig:ai3-conf}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{Phân tích confidence score (218 cylinder + 10 cube)}
  \label{tab:ai3-conf}
  \small
  \begin{tabular}{lcc}
    \toprule
    & \textbf{True Positive (cylinder)} & \textbf{False Positive (cube)} \\
    \midrule
    Score range     & 0.29 -- 0.98       & 0.08 -- 0.36 \\
    Median          & \textbf{0.94}      & --- \\
    $\geq$0.50      & 99.5\% (217/218)   & \textbf{0/10 (0\%)} \\
    $\geq$0.70      & 99.1\% (216/218)   & 0/10 (0\%) \\
    \midrule
    Gap ph\^an t\'ach & \multicolumn{2}{c}{$0.70 - 0.36 = 0.34$} \\
    \bottomrule
  \end{tabular}
\end{table}

3 outlier có score thấp (0.29, 0.61, 0.74) tương ứng với
cylinder bị nhìn từ góc bất thường hoặc bị che khuất. Ỡ
runtime, các trường hợp này fallback về click thủ công ---
không những không nh hưởng đến robot mà còn bảo toàn
nư hành vi ban đầu.


% ----------------------------------------------------------------
\subsection{Kết quả tích hợp hệ thống}
\label{subsec:ai3-integration}
% ----------------------------------------------------------------

\begin{table}[htbp]
  \centering
  \caption{So sánh khả năng trước và sau khi tích hợp AI}
  \label{tab:ai3-comparison}
  \small
  \begin{tabular}{lll}
    \toprule
    \textbf{Tiêu chí}              & \textbf{Hệ thống gốc} & \textbf{Sau cải tiến} \\
    \midrule
    Init mỗi chu kỳ              & Click thủ công       & Tự động (AI) \\
    Tỷ lệ init thành công      & 100\% (manual)      & $\sim$99.5\% (AI) \\
    Fallback khi AI thất bại   & Không có             & Click thủ công \\
    Tracking trong JOINT        & vpDot2              & vpDot2 (giữ nguyên) \\
    Phục hồi khi mất tracking   & Reset toàn bộ       & AI re-init vpDot2 \\
    Chạy không giám sát        & Không               & Có \\
    \bottomrule
  \end{tabular}
\end{table}

Một điểm cần nhấn mạnh: cơ chế tracking trong JOINT
\textbf{hoàn toàn không thay đổi} --- vpDot2 vẫn chạy
mỗi frame như cũ. AI chỉ can thiệp ở đầu chu kỳ
(thay click thủ công) và khi có exception (thay reset
toàn bộ). Latency của AI ($\sim$100--600~ms/lần gọi)
không ảnh hưởng đến servo loop vì nó chỉ xảy ra
từ thựnh thoảng --- không phải mỗi frame.


% ----------------------------------------------------------------
\subsection{Phân tích latency: địa điểm sử dụng và chi phí thực tế}
\label{subsec:ai3-latency}
% ----------------------------------------------------------------

Về mặt latency, điều quan trọng nhất là xác định
\textbf{khi nào AI được gọi} --- vì mỗi lần gọi sinh ra
một Python process mới qua \texttt{popen()}, chi phí không
nhỏ.

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[
    node distance=0.55cm and 1.2cm,
    ev/.style={draw, rounded corners=3pt, fill=gray!8, draw=gray!50,
               text width=4.8cm, align=left, minimum height=0.85cm,
               font=\small, inner sep=7pt},
    ai/.style={ev, fill=pygreen!10, draw=pygreen!55},
    dot/.style={ev, fill=cppblue!10, draw=cppblue!55},
    arr/.style={->, >=Stealth, thick, gray!60},
    lbl/.style={font=\scriptsize, text=gray!70},
  ]

  \node[ev]  (e1) {PREINIT: robot $\to$ home pose};
  \node[ai,  below=of e1] (e2)
        {\textbf{AI gọi lần 1:}~\texttt{detectCylinderWithAI()}\\{\scriptsize $\sim$200--600\,ms --- init vpDot2}};
  \node[dot, below=of e2] (e3)
        {JOINT: vpDot2.track() mỗi frame\\{\scriptsize $\sim$1--5\,ms/frame, liên tục}};
  \node[ai, below=of e3, dashed, draw=pygreen!40, fill=pygreen!5] (e4)
        {\textbf{AI gọi (nếu cần):}~khi \texttt{vpTrackingException}\\{\scriptsize $\sim$200--600\,ms --- re-init vpDot2}};
  \node[dot, below=of e4] (e5)
        {JOINT tiếp tục: vpDot2.track()\\{\scriptsize $\sim$1--5\,ms/frame}};
  \node[ev, below=of e5] (e6)
        {APPROACH $\to$ GRIPPER $\to$ CLASSIFIED};

  \draw[arr](e1)--(e2);
  \draw[arr](e2)--(e3);
  \draw[arr, dashed](e3)--(e4) node[lbl,right,midway]{exception\\(nếu xảy ra)};
  \draw[arr](e4)--(e5);
  \draw[arr](e5)--(e6);

  %% Annotation: AI chỉ xảy ra ở 2 điểm
  \node[font=\scriptsize\itshape, text=pygreen!70, right=0.3cm of e2]
       {$\leftarrow$ 1 lần/chu kỳ};
  \node[font=\scriptsize\itshape, text=pygreen!55, right=0.3cm of e4]
       {$\leftarrow$ chỉ khi exception};

  \end{tikzpicture}
  \caption{Tần suất gọi AI trong một chu kỳ điều khiển.
           AI chỉ xảy ra tối đa 2 lần/chu kỳ: (1)~khởi tạo
           ở đầu, (2)~khi vpDot2 báo lỗi. vpDot2 đảm nhận
           tracking liên tục với latency 1--5\,ms/frame.}
  \label{fig:ai3-timing}
\end{figure}

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      xbar stacked,
      bar width=0.55cm,
      width=11cm, height=5.0cm,
      symbolic y coords={
        {EdgeTPU (gọi khi cần)},
        {CPU (gọi khi cần)}
      },
      ytick=data,
      y tick label style={align=right, font=\small},
      xlabel={Thời gian (ms) mỗi lần gọi AI},
      xlabel style={font=\small},
      xmin=0, xmax=780,
      legend style={at={(0.5,-0.28)}, anchor=north,
                    legend columns=4, font=\scriptsize},
      legend cell align=left,
      grid=major, grid style={dashed, gray!25},
      enlarge y limits=0.4,
    ]
    %% I/O | Inference | Model load | Python startup
    \addplot[fill=gray!40, draw=gray!60] coordinates {
      (10, {CPU (gọi khi cần)})
      (10, {EdgeTPU (gọi khi cần)})
    };
    \addplot[fill=warnred!55, draw=warnred!70] coordinates {
      (150, {CPU (gọi khi cần)})
      (10,  {EdgeTPU (gọi khi cần)})
    };
    \addplot[fill=orng!65, draw=orng!80] coordinates {
      (100, {CPU (gọi khi cần)})
      (50,  {EdgeTPU (gọi khi cần)})
    };
    \addplot[fill=cppblue!55, draw=cppblue!70] coordinates {
      (350, {CPU (gọi khi cần)})
      (350, {EdgeTPU (gọi khi cần)})
    };
    \legend{I/O (JPEG save/read), Inference, Model load, Python startup}
    \end{axis}
  \end{tikzpicture}
  \caption{Phân tích thành phần latency mỗi lần gọi AI detection.
           Python startup ($\sim$350\,ms) chiếm phần lớn overhead.
           Do AI chỉ được gọi từ thựnh thoảng (không phải một lần/frame),
           latency này không cản trở servo loop.}
  \label{fig:ai3-latency}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{Latency mỗi lần gọi \texttt{detectCylinderWithAI()}}
  \label{tab:ai3-latency}
  \small
  \begin{tabular}{lcc}
    \toprule
    \textbf{Thành phần}       & \textbf{CPU}       & \textbf{EdgeTPU} \\
    \midrule
    Python startup            & $\sim$350\,ms      & $\sim$350\,ms \\
    Model load (lần đầu)      & $\sim$100\,ms      & $\sim$50\,ms \\
    Inference                 & $\sim$100--200\,ms & $\sim$4--15\,ms \\
    I/O (JPEG)                & $\sim$10\,ms       & $\sim$10\,ms \\
    \midrule
    \textbf{Tổng / lần gọi}  & $\sim$560--660\,ms & $\sim$415--425\,ms \\
    Tần suất gọi            & \multicolumn{2}{c}{1 lần/chu kỳ (+ khi vpDot2 fail)} \\
    \bottomrule
  \end{tabular}
\end{table}

Vì AI chỉ được gọi \textbf{một lần mỗi chu kỳ} (tại home pose,
trước khi servo bắt đầu) và thỉnh thoảng khi vpDot2 bị mất
tracking, tổng latency 500--660\,ms/lần gọi không cản trở
vòng servo. vpDot2 vẫn chạy $\sim$1--5\,ms/frame với tần
suất đầy đủ trong suốt giai đoạn di chuyển.

\smallskip
\noindent\textit{\small
Lưu ý về số liệu: COCO metrics lấy từ đánh giá hai lớp
(cylinder + cube) trên repository huấn luyện. Phân tích
confidence score lấy từ kiểm tra độc lập trên 218 ảnh
cylinder và 10 ảnh cube (CPU inference via ai-edge-litert).}
